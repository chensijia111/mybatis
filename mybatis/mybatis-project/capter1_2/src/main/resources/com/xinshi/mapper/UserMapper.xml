<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xinshi.mapper.UserMapper" >
    <select id="getUserByUserId" resultType="com.xinshi.pojo.User">
        select * from user where id=#{id};

    </select>
    <select id="getUser2" resultType="com.xinshi.pojo.User">
        select * from user where username=#{arg0} and password=#{arg1};

    </select>
    <select id="getUser3" resultType="com.xinshi.pojo.User">
        select * from user where username=#{param1} and password=#{param2};

    </select>
    <select id="getUser4" resultType="com.xinshi.pojo.User">
        select * from user where username=#{userName} and gender=#{gender};

    </select>
    <select id="getUser5" resultType="com.xinshi.pojo.User">
        select * from user where username=#{firstUser.username} or username=#{secondUser.username};
    </select>
    <select id="getUser6" resultType="com.xinshi.pojo.User">
        select * from user where username=#{userName1} or username=#{userName2};
    </select>
    <select id="getUser7" resultType="com.xinshi.pojo.User">
        select * from user where username=#{userName1} or username=#{userName2};
    </select>
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        insert into user(username,password,gender) values(#{user.username},#{user.password},#{user.gender});
    </insert>
    <update id="updateUser">
        update user set username=#{userName},gender=#{gender} where id=#{id}
    </update>
    <delete id="deleteUser">
        delete from user where id=#{id}
    </delete>
</mapper>