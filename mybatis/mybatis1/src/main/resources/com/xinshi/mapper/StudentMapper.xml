<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xinshi.mapper.StudentMapper" >
    <select id="getStudentyStudentId" resultType="com.xinshi.pojo.Student">
        select * from student where id=#{id};

    </select>
    <insert id="insertStudent" parameterType="com.example.entity.Student">
        insert into student(name,age,gender,chinese,english) values(#{student.name},#{student.age},#{student.gender),
        #{student.chinese},#{student.english});
    </insert>





</mapper>